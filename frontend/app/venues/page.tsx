import VenueBrowseContent from '@/components/VenueBrowseContent';

export const metadata = {
  title: 'Browse Venues | Live Recordings | 8PM',
  description: 'Browse venues where live concert recordings were captured. Find shows by location, discover new venues, and explore the jam band scene.',
};

export default function VenuesPage() {
  return <VenueBrowseContent />;
}
